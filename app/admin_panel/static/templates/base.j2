<!DOCTYPE html>
<html lang="en" style="height: 100%;">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-SgOJa3DmI69IUzQ2PVdRZhwQ+dy64/BUtbMJw1MZ8t5HZApcHrRKUc4W0kG879m7" crossorigin="anonymous">
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Rubik:ital,wght@0,300..900;1,300..900&display=swap"
        rel="stylesheet">
    <title>Reversl Admin Panel</title>
    <style type="text/css">
        :root {
            --bs-body-font-family: "Rubik", sans-serif;
            --bs-border-radius: 0.5rem;
        }

        html,
        body {
            height: 100%;
            width: 100%;
            overflow: hidden;
            margin: 0;
            padding: 0;
        }

        header {
            height: 6%;
            z-index: 5;
            padding: 0 1%;
            display: flex;
            flex-direction: row;
            align-items: center;
            justify-content: space-between;
        }

        main {
            height: calc(100% - 6%);
            width: 100%;
            z-index: 4;
            display: flex;
            align-items: center;
            justify-content: center;
            background-image: url("https://papik.pro/uploads/posts/2021-11/1636593442_40-papik-pro-p-html-risunok-fona-41.jpg");
            background-size: cover;
        }

        header a.active {
            border-bottom: 1px solid black;
        }

        .scrollarea {
            scrollbar-width: thin;
            /* Firefox */
            scrollbar-color: rgba(0, 0, 0, 0.15) transparent;
            /* Firefox */
        }

        /* Chrome, Safari, Edge */
        .scrollarea::-webkit-scrollbar {
            width: 6px;
        }

        .scrollarea::-webkit-scrollbar-thumb {
            background-color: rgba(0, 0, 0, 0.15);
            border-radius: 4px;
        }

        .scrollarea::-webkit-scrollbar-track {
            background: transparent;
        }

        .btn {
            --bs-btn-font-family: "Rubik", sans-serif;
        }

        .list-group {
            --bs-list-group-active-bg: white;
            --bs-list-group-active-color: black;
            --bs-list-group-bg: #ebebeb;
            --bs-list-group-border-width: 0;
        }

        .file-item {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 5px 10px;
            background-color: #f8f9fa;
            border-radius: 4px;
            margin-bottom: 5px;
        }

        .file-item span {
            flex: 1;
            overflow: hidden;
            text-overflow: ellipsis;
            white-space: nowrap;
        }

        .file-item button {
            margin-left: 10px;
        }

        /* Скрываем input file */
        #file-input {
            display: none;
        }
    </style>
</head>

<body>
    {% block main %}
    {% endblock %}
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-k6d4wzSIapyDyv1kpU366/PK5hCdSbCRGRCMv+eplOQJWyd1fbcAu9OCUj5zNLiq"
        crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.8/dist/umd/popper.min.js"
        integrity="sha384-I7E8VVD/ismYTF4hNIPjVp/Zjvgyol6VFvRkX/vR+Vc4jQkC+hVqc2pM8ODewa9r"
        crossorigin="anonymous"></script>
</body>

</html>